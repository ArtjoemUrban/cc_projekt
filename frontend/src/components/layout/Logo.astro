---
// Generiert mit Claude Opus 4.8
import FSELogo from "../../assets/FSE_Logo_SVG.svg";

interface Props {
	size?: number;
}

const { size = 40 } = Astro.props;
---

<img
	src={FSELogo.src}
	alt="Fachschaft E Logo"
	class="shrink-0"
	style={`width:${size}px;height:${size}px`}
/>
